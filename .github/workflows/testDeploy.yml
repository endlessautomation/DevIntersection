name: Deploy to Test

on:
  pull_request:
    branches: [ "test" ]

  workflow_dispatch:
jobs:
 buildCode:
  name: Build SQL Projects
  runs-on: self-hosted
  steps:
  - uses: actions/checkout@v3

  - uses: azure/sql-action@v2.2
    with:
      connection-string: "server=localhost;database=AWTest;User ID=deployUser;PWD=SneakyP@ssw0rd;"
      action: publish
      path: './AdventureWorks\AdventureWorks.sqlproj'
